<?php
$config = array(
    /*1 => array(
        'charset'        => 'GBK',                              //客户端字符集,如:'UTF-8','GBK'(大写)
        'debug'          => false,                              //与服务端通信时打印原始信息(开启后会强制使用同步请求)
        'enableRequest'  => true,                               //是否像客户端发送信息,true=是,false=否
        'enableResponse' => true,                               //是否接收客户端的信息,true=是,false=否
        'key'            => '123456789',                        //通信密钥,要与服务端相同
        'superClient'    => false,                              //是否可以定制sql语句和方法,true=是,false=否(由本项导致的相关问题与大连奥瑞文网络技术有限公司无关,包括但不限于用户隐私泄露,客户端像服务端挂马,破坏服务端数据)
        'timeout'        => '30',                               //服务端向客户端通信超时时间(秒)
        'url'            => 'http://localhost/cs/oSync',        //客户端插件跟目录(最后没有'/')
    ),
    2 => array(
        'charset'        => 'GBK',                              //客户端字符集,如:'UTF-8','GBK'(大写)
        'debug'          => false,                               //与服务端通信时打印原始信息
        'enableRequest'  => true,                               //是否像客户端发送信息,true=是,false=否
        'enableResponse' => true,                               //是否接收客户端的信息,true=是,false=否
        'key'            => 'v1234567890',                      //通信密钥,要与服务端相同
        'superClient'    => false,                              //是否可以定制sql语句和方法,true=是,false=否
        'timeout'        => '30',                               //服务端向客户端通信超时时间(秒)
        'url'            => 'http://localhost/test/oSync',      //客户端插件跟目录(最后没有'/')
    )    // */
);